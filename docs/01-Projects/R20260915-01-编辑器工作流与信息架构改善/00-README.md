# 编辑器工作流与信息架构改善（Screenshot Studio）

本夹是 PARA `01-Projects` 下的一个项目：把 Screenshot Studio **"功能齐但不好用"** 的问题查清楚，
按用户真实的核心工作流给出**信息架构（IA）重排方案 + 引导式导入方案**。

- 提出人：廖海（liaohai1）· 简体中文用户
- 创建日：2026-09-15
- 对象仓库：`D:\code\screenshot-studio`（分支 `dev-creator`，基线 `760dddf`）
- 当前状态：**需求 / 调查 / 诊断 / 方案已完成；A 档已施工并验证**（P0 全量 + 模块 1 + 模块 3）

## 文档

| 文件 | 内容 |
|------|------|
| [01-需求_编辑器核心工作流与信息架构.md](./01-需求_编辑器核心工作流与信息架构.md) | 用户原话、需求 R1–R8、核心旅程 J1–J7、范围、7 个待确认项、5 条验收标准 |
| [02-调查_现有工作流与信息架构取证.md](./02-调查_现有工作流与信息架构取证.md) | 15 条导入链路、背景面板 318 个目标解剖、文字/标注/图层链路、导出链路、14 处死代码、类型检查实测、6 项未确认 |
| [03-分析报告_现状诊断.md](./03-分析报告_现状诊断.md) | 结论先行、J1–J7 逐步诊断、七类根因、"排版怪"具体化 10 条、**阻断性发现**、需求对照表、优先级建议 |
| [04-方案_导入引导与信息架构重排.md](./04-方案_导入引导与信息架构重排.md) | 6 条设计原则、目标 IA 图、A/B/C 三档方案对比、方案 B 七模块详设、P0–P3 实施路径、风险与回滚、待确认项 |
| [05-执行记录_工作流与IA落地.md](./05-执行记录_工作流与IA落地.md) | **A 档施工结果**：改前改后对照、逐条回填证据编号、验证数据、明确留白、已知不足 |
| [06-走查_启动与验证指南.md](./06-走查_启动与验证指南.md) | **动手验一遍的入口**：怎么启动、5 道自动化门、12 步手工清单、控制台什么算问题、不通过时怎么定位 |

## 一句话结论

**功能不是不够，而是没有为任何一条用户旅程编排过。**

15 条导入入口、318 个背景候选（全部无名 32px 色块）、14 处死代码 UI、3 套"有没有图"的判据、
桌面 5 个 / 移动 6 个互不一致的 Tab，全部平铺在同一个 260px 侧栏 + 一个 340px 弹窗里。

用户面对的不是"功能少"，而是**每一步都要先做一次考古**：

- **不知道怎么上传**：头部**没有**「导入」按钮；唯一入口绑在"空画布"状态上，有图即消失；
  而空画布时左右两个面板**照样全量渲染**，一堆控件可点但什么都不发生。
- **改背景按钮不方便**：318 个候选堆在约 212px 宽的纵向长条里，渐变色块 32px、**无名字**、
  **无 hover 预览**、无搜索、无收藏、滚动条被 CSS 隐藏、11 个分组每次进来全展开、
  从"光与影"滚到"渐变"约 3 屏，两个横向画廊分别有 4.7 屏和 10.7 屏的内容。

**病根：功能按"实现分类"堆叠，没有按"用户动作顺序"编排。**

## ✅ 两个前置事项已处理（2026-09-15）

### 1. 工作树已被冻结为基线，残留回归已修

调查期间另一个会话正在同一工作树里跑一套 i18n 流水线，4 分钟内两次全量改写 160+ 个文件，
并引入 7 处"值级误译"（`tsc` 抓不到这类问题）。处理结果：

- 流水线已停；汉化工作树（162 个改动文件 + 工具链 + 字典）已提交为 `760dddf`，`git status` 干净。
- 7 处回归中最后残留的 1 处（`TransformsGallery.tsx` 的 `'热门'` vs `'Popular'`）已修，
  改法是给分类加**稳定 `id`**，逻辑一律不按展示名匹配 —— 以后改文案不会再静默失效。
- 以"在数据表里按 `name` 值做匹配"为线索做了全仓巡检：`.name ===` / `switch (中文)` /
  `as const` 数组含中文 / `lib/**` 中文字面量比较，**命中均为 0**。
- `npx tsc --noEmit` 现为 **0 error**（改前 1 条，是 `tests/` 与 `scripts/` 两个全局脚本
  重复声明 `const BASE`）。

详见 [05-执行记录_工作流与IA落地.md](./05-执行记录_工作流与IA落地.md) §2.2。

### 2. 排版"怪怪的"已落到可改条目并部分落地

`03-分析报告` §4 的 10 条里，第 1/2/3/4/9 条（同级不同形、选中态两套语言、分组无层次、
折叠状态不记忆、触摸目标过小）已在背景面板重构中处理；其余随模块 4/5/6 排期。

## 已交付（A 档）

按 `04-方案` §5 的 **P0 全量 + 模块 1 + 模块 3**：

```
① 导入图片  →  ② 调整图片  →  ③ 设置背景  →  ④ 加文字与标注  →  ⑤ 导出
   ✅ 常驻入口      ⚠️ 未重排        ✅ 已重构        ❌ 未做          ❌ 未做
   ✅ 引导式首屏
   ✅ 换主图保留样式
```

- **P0**：冻结基线、修残留回归、修「换主图」（新增 `replaceMainImage`）、空画布收起无效面板。
- **模块 1**：头部常驻「导入 / 替换图片」、首屏「把图片放进来」三入口、导入后"下一步"指引。
- **模块 3**：背景面板改分类 Tab + 命名候选 + 搜索 + 最近/收藏 + 背景不透明度/模糊/颗粒。

## 未交付（明确留白）

| 模块 | 内容 |
|---|---|
| 模块 4 | 步骤条 + 单侧属性面板布局（唯一一次真正的布局重构，风险集中点，建议单独排期） |
| 模块 5 | 文字工具补旋转/对齐/层级；标注连续绘制；图层重命名/锁定/排序 |
| 模块 6 | 导出改弹窗 + 用途化文案 + 尺寸/体积预估；GIF 入口（`llms.txt` 承诺与界面现状的矛盾仍在） |
| 模块 7 | 归档 14 处死代码 UI；草稿状态可见化；移除编辑器内营销横幅 |

验收自检（`01-需求` §7 五条）：**2 条完全通过、3 条部分通过** —— 详见 `05-执行记录` §3。

## 下一步

**想先自己验一遍** → 直接看 [06-走查_启动与验证指南.md](./06-走查_启动与验证指南.md)，
照附录 A 的一页纸清单走，约 12 分钟。

**想继续施工** → 建议顺序：**模块 4（布局）→ 模块 6（导出）→ 模块 7（清理）**。
模块 4 单独一个 PR、只做布局不夹带其他改动（`04-方案` §6.2 R-1）。

## 与相邻主题的关系

- `R20260914-01-简体中文界面`：负责"界面为什么是英文"的根因与汉化路线。
  其结论（渲染层从未接线、`useTranslations` 命中 0）仍然成立。
  其**施工方式（脚本批量替换硬编码字符串）曾是本主题基线不稳定的来源**，
  现已通过"冻结基线 + 修回归 + 全仓巡检"闭环，并在本轮一并补齐了该主题的已知不足
  （`/features` 标签、`/` 页设备边框名、`/changelog` 英文日期、函数参数默认值漏译）。
- **已按建议串行执行**：汉化回归修复 → 冻结基线 → 本主题施工。
- **文案策略仍未定**：目前是"硬编码中文"。若日后要接 next-intl，本主题新增的文案
  也需要一并改成 key；已刻意把新文案集中在新文件（`import-image.ts`、`background-prefs.ts`、
  `PostImportHint.tsx`）里，降低后续迁移成本。

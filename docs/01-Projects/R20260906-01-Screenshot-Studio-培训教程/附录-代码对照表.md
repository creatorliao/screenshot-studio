# 附录 · 代码对照表

> 想看实现的人，从模型节点跳到源码。只列「已证实」的路径，不在这里写操作步骤。
> 本表从 `03-分析稿.md` §7.1 剪出。

---

## 模型节点 → 界面入口 → 关键路径

| 模型节点 | 界面入口 | 关键路径 |
|----------|----------|----------|
| ① 获得画面 | 空态上传区 | `components/controls/CleanUploadState.tsx`、`components/editor/UploadArea.tsx` |
| ① 网页截图 | URL 输入框 | `components/controls/WebsiteScreenshotInput.tsx`、`app/api/screenshot/route.ts` |
| ① 推文导入 | Add a Tweet | `components/editor/sections/TweetImportSection.tsx`、`app/api/tweet/[id]/route.ts` |
| ② 定画幅 | Header 画幅选择器 | `components/editor/EditorHeader.tsx`、`lib/constants/aspect-ratios.ts`、`components/aspect-ratio/` |
| ③ 浏览器样机 | 左栏 Browser 模式 | `components/editor/sections/BrowserMockupSection.tsx` |
| ③ 设备样机 | 左栏 Device 模式 | `components/editor/sections/DeviceFramesSection.tsx`、`lib/constants/mockups.ts`、`components/mockups/` |
| ③ 图片样式 / 圆角 | Design 面板 Style / Border | `components/editor/sections/StyleSection.tsx`、`BorderSection.tsx` |
| ④ 背景 | 左栏 BG 标签 | `components/editor/sections/BackgroundSection.tsx`、`lib/constants/backgrounds.ts`、`lib/r2-backgrounds.ts`、`lib/constants/gradient-colors.ts`、`lib/constants/mesh-gradients.ts` |
| ④ 阴影 | Design 面板 Shadow | `components/editor/sections/ShadowSection.tsx` |
| ④ 贴纸 / 3D 物体 / 图层 | Stickers、Layers 标签 | `components/editor/sections/ImageOverlaySection.tsx`、`DepthSection.tsx` |
| ④ 滤镜 / 画布圆角 | Design 面板 Settings | `components/editor/sections/SettingsSection.tsx` |
| ⑤ 标注 | Draw & Markup | `components/editor/sections/AnnotateSection.tsx` |
| ⑤ 文字 | Add Text | `components/editor/sections/TextSection.tsx`、`components/text-overlay/` |
| ③/④ 3D 变换 | 右栏 3D 标签 | `components/editor/RightSettingsPanel.tsx` |
| ⑥ 动画 | 右栏 Motion、Animate、时间轴 | `lib/animation/presets.ts`、`components/timeline/` |
| ⑦ 图片导出 | Header Save | `components/canvas/dialogs/ExportDialog.tsx`、`components/export/`、`hooks/useExport.ts`、`lib/export/` |
| ⑦ 视频导出 | Header Export Video | `lib/export-slideshow-dialog.tsx`、`lib/export/video-encoder.ts`、`lib/export/ffmpeg-encoder.ts` |
| 支线 代码出图 | `/code` | `app/[locale]/code/page.tsx`、`components/code-image/` |
| 辅助 草稿 / 偏好 | 自动 | `hooks/useAutosaveDraft.ts`、`lib/draft-storage.ts`、`lib/export-storage.ts` |

---

## 常用常量 / 状态位置

| 想看什么 | 路径 |
|----------|------|
| 上传限制（100MB、支持类型） | `lib/constants.ts` |
| 画幅清单 | `lib/constants/aspect-ratios.ts` |
| 设备样机与布局（MAX_DEVICE_MOCKUPS=6） | `lib/constants/mockups.ts` |
| 背景分类与图片清单 | `lib/r2-backgrounds.ts` |
| 渐变 / 纯色 / mesh / magic | `lib/constants/gradient-colors.ts`、`solid-colors.ts`、`mesh-gradients.ts` |
| 动画预设与分类 | `lib/animation/presets.ts` |
| 代码主题 / 语言 / 字体 | `components/code-image/code-themes.ts`、`code-themes-data.ts` |
| 全局状态（Zustand + zundo 撤销） | `lib/store/index.ts` |
| 导出类型与质量预设 | `lib/export/types.ts` |

---

## 未证实 / 与 README 差异（只查证，不进教程正文）

| 说法 | 事实 |
|------|------|
| README「32 syntax themes」 | 代码 14 个主题（`code-themes-data.ts`） |
| README「export up to 5x」 | 编辑器 UI 1x/2x/3x（`ScaleSlider.tsx`）；代码页 2x/4x |
| README「MP4, WebM, GIF」 | UI 只暴露 MP4/WebM；GIF 在 `ffmpeg-encoder.ts` 但 UI 未暴露 |
| README「Arc / Polaroid 浏览器样机」 | 当前编辑器 Browser 模式只有 Safari/Safari Dark/Chrome/Chrome Dark；Arc/Photograph 帧在 `components/controls/BorderControls.tsx`，未被当前 `EditorLayout` 挂载 |
| `.env.example` 存在 | 仓库未提交（`.gitignore` 忽略 `.env*`） |

> 想进一步下探项目整体结构，读 `CONTRIBUTING.md` 的 Project Structure；想了解架构，读仓库根 `ARCHITECTURE.md`。
